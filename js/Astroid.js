class Astroid extends Attacker {
  constructor(width, height, color, x, y, speed, angle) {
    super();
    this.width = width;
    this.height = height;
    this.x = x;
    this.y = y;
    this.speed = speed;
    this.angle = angle;
    this.color = color;
  }
}
